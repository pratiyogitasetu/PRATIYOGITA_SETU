/**
 * Firebase config stub — Firebase has been removed.
 * Re-add Firebase integration here when ready.
 */

export const auth = null;
export const db = null;
export default null;
export default app;
